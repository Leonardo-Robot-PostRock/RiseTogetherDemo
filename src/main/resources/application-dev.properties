# === Profile: dev ===
# Uso local: docker compose up db && ./mvnw spring-boot:run -Dspring-boot.run.profiles=dev

# Database (localhost porque MySQL corre en Docker mapeando puerto 3306)
spring.datasource.url=jdbc:mysql://localhost:3306/itjobs?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=utf8
spring.datasource.username=root
spring.datasource.password=fucks0101
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JWT
jwt.secret=DevSecretKeyForJWT2024NotForProductionUse

# Logging
logging.level.root=INFO
logging.level.com.ITJobsBackend=DEBUG
